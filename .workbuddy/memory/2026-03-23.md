# 2026-03-23 工作日志

## 完成剩余功能开发（第三轮）

### 修复的 Bug
1. **PushNotificationRepositoryImpl 拼写错误**：将 `_notachments` 修正为 `_notifications`（原有编译失败 bug）

### 新增 / 完善的功能

2. **ResultViewModel 新建**：`ui/practice/.../ResultViewModel.kt`
   - `@HiltViewModel` + `SavedStateHandle` 接收 `evaluationId`
   - `init {}` 自动加载，`loadResult()` 通过 `EvaluationUseCase.getEvaluation()` 读取本地 Room 数据

3. **ResultFragment 新建**：`ui/practice/.../ResultFragment.kt`
   - 展示总分（64sp，动态颜色：≥80 蓝，≥60 橙，否则红）
   - 三维度分数（发音/流利度/内容）
   - 逐句详情（动态构建 LinearLayout 卡片，逐词颜色标注）
   - MediaPlayer 录音回放（切换播放/停止，onDestroyView 时释放资源）
   - "再练一次" → `popBackStack()` 返回练习页

4. **nav_graph.xml 补全**：
   - `practiceFragment` 节点添加 `<action>` 跳转到 `resultFragment`
   - 新增 `resultFragment` 节点（带 `evaluationId`、`sessionId` 两个参数）

5. **TingwuMessagingService 完善**：
   - 注入 `PushNotificationRepositoryImpl`，替换原有 Log-only 实现
   - FCM Token 刷新时自动调用 `registerDeviceToken()`
   - 消息接收时触发应用内回调 + 展示系统通知 + 处理 deep_link

6. **EvaluationRepository DI 绑定**：在 `RepositoryModule` 中添加 `provideEvaluationRepository()`，绑定 `EvaluationRepositoryImpl`

7. **SyncWorker 改用 @HiltWorker**：
   - `@HiltWorker` + `@AssistedInject`（正式的 Hilt Worker 注入方式）
   - 完善了 `session_result` / `practice_audio` 两种数据类型的处理逻辑

8. **HiltWorkerFactory 配置**：
   - `TingwuApplication` 实现 `Configuration.Provider`，注入并注册 `HiltWorkerFactory`
   - `AndroidManifest.xml` 禁用默认 WorkManager 初始化器（通过 `tools:node="remove"`）

### 依赖修复

9. **Navigation Component 缺失**：
   - `gradle.properties` 添加 `NAVIGATION_VERSION=2.7.7`
   - `build.gradle.kts`（根）添加 Safe Args 插件声明
   - `app/build.gradle.kts` 添加 `navigation-fragment-ktx`、`navigation-ui-ktx`、Safe Args 插件、`hilt-work`、`startup-runtime`
   - `ui/practice/build.gradle.kts` 添加 navigation 和 core:evaluation 依赖
   - `ui/auth/build.gradle.kts` 添加 navigation 依赖
   - `core/offline-sync/build.gradle.kts` 添加 `hilt-work`

10. **strings.xml 补全**：
    - `app/strings.xml` 添加 result_* 系列字符串
    - `ui/practice/src/main/res/values/strings.xml` 新建，包含 PracticeFragment + ResultFragment 全部字符串资源（解决跨模块 R 类访问问题）
    - `ui/practice/src/main/res/values/navigation_ids.xml` 新建，声明 `action_practiceFragment_to_resultFragment` ID（解决跨模块 Safe Args 依赖问题）
