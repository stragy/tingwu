# 2026-03-29 工作日志

## Android + 后端 API 全链路对接（续）

### 完成内容

1. **build.gradle.kts（app 模块）**
   - 启用 `buildConfig = true`
   - `debug` buildType 添加 `buildConfigField("String", "API_BASE_URL", "\"http://10.0.2.2:80/\"")`
   - `release` buildType 添加 `buildConfigField("String", "API_BASE_URL", "\"https://api.tingwu.com/\"")`
   - Android 模拟器访问宿主机用 `10.0.2.2`；真机调试需换成局域网 IP

2. **NetworkModule.kt**
   - `provideRetrofit` 改用 `BuildConfig.API_BASE_URL`（Debug 走本地，Release 走生产）
   - `HttpLoggingInterceptor` 改为 Debug 输出 BODY，Release 仅输出 BASIC（防止 token 泄漏）

3. **Nginx（packages/gateway/nginx/nginx.conf）**
   - 新增 `location /auth/` → auth_service（限流：auth_limit，burst=5）
   - 新增 `location /notification/` → auth_service（限流：api_limit，burst=20）
   - 解决了 Android ApiClient 调用 `/auth/request-sms`、`/auth/verify-sms`、`/auth/refresh`、`/auth/logout`、`/notification/device/register` 等路径在 Nginx 中没有路由规则的问题

### 验证通过

- `ExerciseRepositoryImpl` 数据格式与 practice-service 响应格式完全匹配（`{code, message, data:{items, total, page, pageSize}}`）
- auth-service 中的 Android 适配端点（`/auth/*` 和 `/notification/*`）均已有对应 Nginx 规则
- 整条链路：Android → Nginx 网关 → auth/practice/evaluation 服务，路由全部对齐
